module.exports = {
  blue: (str) => str,
  yellow: (str) => str,
  red: (str) => str,
  green: (str) => str,
  default: {
    blue: (str) => str,
    yellow: (str) => str,
    red: (str) => str,
    green: (str) => str,
  }
};